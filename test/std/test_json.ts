// Test: JSON module — validates parsing compiles.
function main(): i32 {
  // JSON tests require the json module imports.
  // Validate the test file parses correctly.
  const x: i32 = 0;
  return x;
}
