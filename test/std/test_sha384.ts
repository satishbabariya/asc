// RUN: %asc check %s
// Test: SHA-384 — validates the module structure compiles.
function main(): i32 {
  // SHA-384 test requires the crypto module.
  // Validate that the test file itself parses correctly.
  return 0;
}
